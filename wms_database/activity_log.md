# Database Activity Log

- Initialized `schema.sql` with Enums for roles, statuses, priorities, and categories.
- Created `users`, `tickets`, and `comments` tables.
- Applied Row Level Security (RLS) policies.
- Inserted demo data for `users` and a few initial `tickets`.
